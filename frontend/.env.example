# ═══════════════════════════════════════════════════════════
# Frontend — Example Environment Variables (Copier vers .env)
# 
# IMPORTANT:
# 1. Copier ce fichier : cp .env.example .env
# 2. Éditer .env avec vos valeurs réelles
# 3. NE JAMAIS commiter .env (voir .gitignore)
# 4. Cette variable est utilisée au moment du build React
# ═══════════════════════════════════════════════════════════

# ── API Backend URL (vue par le navigateur/React) ──
# Local: http://localhost:5000/api
# Staging: https://api-staging.orthoshop.com/api
# Production: https://api.orthoshop.com/api
REACT_APP_API_URL=http://localhost:5000/api

# ── Environment (utilisé par React) ──
NODE_ENV=development
