# # ═══════════════════════════════════════════════════════════
# # Backend — Example Environment Variables (Copier vers .env)
# # 
# # IMPORTANT:
# # 1. Copier ce fichier : cp .env.example .env
# # 2. Éditer .env avec vos valeurs réelles
# # 3. NE JAMAIS commiter .env (voir .gitignore)
# # ═══════════════════════════════════════════════════════════

# # ── MongoDB URI ──
# # Local: mongodb://127.0.0.1:27017/orthopedic_shop
# # Docker with auth: mongodb://admin:password@mongo:27017/orthopedic_shop?authSource=admin
# MONGO_URI=mongodb://127.0.0.1:27017/orthopedic_shop

# # ── JWT Secret (32 bytes minimum, générer: openssl rand -base64 32) ──
# # En production, utiliser une clé forte et unique
# JWT_SECRET=your_very_long_random_jwt_secret_here_change_in_production

# # ── Server ──
# PORT=5000

# # ── Environment (development, staging, production) ──
# NODE_ENV=development

# # ── CORS — Origines autorisées (domaine du frontend) ──
# # Exemples: https://orthoshop.com,https://www.orthoshop.com
# CORS_ORIGIN=*

# # ── Logs ──
# LOG_LEVEL=debug
