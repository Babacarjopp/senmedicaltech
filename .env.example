# # ═══════════════════════════════════════════════════════════
# # OrthoShop — Example Environment Variables (Copier vers .env)
# # 
# # IMPORTANT:
# # 1. Copier ce fichier : cp .env.example .env
# # 2. Éditer .env avec vos valeurs
# # 3. NE JAMAIS commiter .env (voir .gitignore)
# # 4. En production, générer JWT_SECRET fort: openssl rand -base64 32
# # ═══════════════════════════════════════════════════════════

# # ── JWT Secret (32 bytes minimum, générer: openssl rand -base64 32) ──
# JWT_SECRET=your_very_long_random_jwt_secret_here_change_in_production

# # ── MongoDB Authentication ──
# MONGO_USER=admin
# MONGO_PASSWORD=your_strong_mongo_password_here

# # ── Node Environment (development, staging, production) ──
# NODE_ENV=development

# # ── CORS — Origines autorisées (séparées par des virgules) ──
# # Exemples: http://localhost:3000,https://SenMedicaltech.com,https://www.SenMedicaltech.com
# CORS_ORIGIN=http://localhost:3000

# # ── Frontend API URL (vue par le navigateur/React) ──
# # Local: http://localhost:5000/api
# # Production: https://api.SenMedicaltech.com/api
# REACT_APP_API_URL=http://localhost:5000/api

# # ── Server Port ──
# PORT=5000

# # ── Log Level (debug, info, warn, error) ──
# LOG_LEVEL=debug

# # ── Database Backups (optionnel) ──
# BACKUP_ENABLED=false
# BACKUP_PATH=/data/backups

# # ── Session & Security (optionnel) ──
# # SESSION_SECRET=your_session_secret_here
# # REDIS_URL=redis://localhost:6379

# # ── Mail (SMTP) ──
# # Développement: utiliser Mailtrap, Gmail SMTP, ou MailHog
# # Production: SendGrid, Mailgun, AWS SES ou autre fournisseur
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=587
# MAIL_USER=your_mail_user
# MAIL_PASS=your_mail_password
# MAIL_FROM="OrthoShop <no-reply@orthoshop.local>"

# # ── Seeder (développement UNIQUEMENT) ──
# # Ne JAMAIS exécuter le seeder en production
# # Créer les comptes admin via une autre méthode (CLI, panneau d'admin, etc.)
